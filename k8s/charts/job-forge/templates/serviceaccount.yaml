{{/*
Un ServiceAccount por servicio, nunca el default.

No es burocracia: el ServiceAccount es el sujeto al que en EKS se le cuelga el
rol de IAM vía IRSA (F6). Compartir uno significa que todos los servicios acaban
con la unión de los permisos que necesita el más goloso.

Ninguno de los cuatro llama a la API de Kubernetes, así que no se les monta el
token. Un token montado sin usar es solo credencial que un atacante encuentra.
*/}}
{{- range $component := list "api" "worker" "audit" "ui" }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "job-forge.fullname" $ }}-{{ $component }}
  labels:
    {{- include "job-forge.labels" (dict "root" $ "component" $component) | nindent 4 }}
  {{- with $.Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ $.Values.serviceAccount.automountServiceAccountToken }}
{{- with $.Values.imagePullSecrets }}
# Colgado del ServiceAccount y no de cada Deployment: asi el kubelet lo usa para
# CUALQUIER pod que use esta identidad, sin repetir la referencia cuatro veces.
#
# Solo hace falta fuera de EKS. El kubelet de EKS trae un proveedor de
# credenciales que cambia el rol del nodo por un token de ECR sin que nadie se lo
# pida; k3s no, y por eso en el demo publico un temporizador del nodo mantiene
# vivo este Secret. En kind se queda vacio: las imagenes se cargan a mano.
imagePullSecrets:
  {{- toYaml . | nindent 2 }}
{{- end }}
{{- end }}
